# pnpm configuration

auto-install-peers=true
strict-peer-dependencies=false

allow-builds=@prisma/engines prisma esbuild sharp unrs-resolver

prefer-offline=true
save-exact=true
use-lockfile=true
shared-workspace-lockfile=true
